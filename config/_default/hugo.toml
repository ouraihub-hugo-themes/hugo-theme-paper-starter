# Hugo Paper Theme - Core Configuration
# This file contains core Hugo settings

baseURL = "https://hugopaper.github.io/"
title = "Hugo Paper"

# Content Settings
enableInlineShortcodes = true
enableEmoji = true
hasCJKLanguage = true
summaryLength = 200
defaultContentLanguage = "en"

# Pagination
[pagination]
pagerSize = 4

# Taxonomies
[taxonomies]
category = "categories"
tag = "tags"

# Outputs
[outputs]
home = ["HTML", "RSS", "JSON"]
page = ["HTML"]
section = ["HTML", "RSS"]

# Output Formats
[outputFormats.JSON]
mediaType = "application/json"
baseName = "index"

# Minify Configuration
[minify]
  [minify.tdewolff]
    [minify.tdewolff.html]
      keepWhitespace = false
    [minify.tdewolff.json]
      # Disable JSON minification to avoid parsing errors
      precision = 0

# Server
[server]
port = 1313
